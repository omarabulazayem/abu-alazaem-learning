# Live source patches

Railway extracts the original `source.tgz` and then overlays the files in this directory. This keeps the current migration work reviewable while the repository is being normalized into a regular source tree.

Current patch set: Supabase Auth/PostgreSQL foundation, parent+child account model, teacher dashboard, classes, secure progress/reward backend, and Supabase SQL migration.
